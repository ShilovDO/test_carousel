server {
    listen 80;
    server_name 176.108.249.27;  # Явно укажите ваш IP
    client_max_body_size 500M;
    
    # Добавлено: правильные MIME-типы для изображений
    types {
        image/webp webp;
        image/jpeg jpg jpeg;
        image/png png;
        image/gif gif;
    }
    
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
    
    # Добавлено: специальная обработка для папки images
    location /images/ {
        root /usr/share/nginx/html;
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }

    location /api/ {
        proxy_pass http://carouselbackend:5087/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Критически важно для кук:
        proxy_cookie_domain ~^(.*)$ "176.108.249.27";
        proxy_cookie_path / "/;";
        proxy_pass_header Set-Cookie;
        proxy_set_header Cookie $http_cookie;
        
        add_header Access-Control-Allow-Credentials "true";
        add_header Access-Control-Expose-Headers "Set-Cookie";
    }
}